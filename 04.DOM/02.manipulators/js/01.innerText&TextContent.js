const h1 = document.querySelector('h1');
const ul = document.querySelector('ul');
const p = document.querySelector('#main');

// innerText
// sellects the text in the h1 tag
h1.innerText = "I'm Hungry!";
// sellects the text in the ul tags and merges it into one string
ul.innerText;

// textContent
// shows everything like it is in the html and even display none items
p.textContent;
